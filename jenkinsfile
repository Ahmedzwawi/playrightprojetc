pipeline {
    agent {
        docker {
            image 'mcr.microsoft.com/playwright:v1.50.0-noble'
        }
    }
    stages {
        stage('Run') {
            steps {
                sh 'npm ci'
                sh 'npx playwright test'
            }
        }

       
    }

  
}
